apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: hello
  annotations:
    configsync.gke.io/cluster-name-selector: anand-cluster
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
